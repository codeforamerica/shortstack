<%= form_for(@org_type) do |f| %>
  <% if @org_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@org_type.errors.count, "error") %> prohibited this org_type from being saved:</h2>

      <ul>
      <% @org_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p><%= f.label :name %>: <%= f.text_field :name %></p>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
