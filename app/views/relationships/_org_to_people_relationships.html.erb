<% form_for [item, Relationship.new], :html => {:id =>"people-relationship-form", :style => "display:none;" } do |f| -%>
<fieldset>
  <legend>Add a Person Relationship</legend>
  <p><%= select("relationship", "person_id", Person.all.collect {|p| [ p.name, p.id ] })%> <%= select("relationship", "relation_type", RelationType.where("type_name = 'Person'").all.collect {|p| [ p.name, p.name ] })%> this organization</p>
  </p>
  <a href="#" onClick="$('#product-relationship-form').hide();" id="hide">Hide Form</a>  
  <p>
    <%= f.submit 'Create' %>
  </p>
</fieldset>  
<% end %>
