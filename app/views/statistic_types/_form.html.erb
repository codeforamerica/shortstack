<%= form_for(@statistic_type) do |f| %>
  <% if @statistic_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@statistic_type.errors.count, "error") %> prohibited this statistic_type from being saved:</h2>

      <ul>
      <% @statistic_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p><%= f.label :name %>: <%= f.text_field :name %></p>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
